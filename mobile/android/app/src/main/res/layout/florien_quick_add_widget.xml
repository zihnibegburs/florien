<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/florien_widget_background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/quick_add_prompt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_quick_prompt"
        android:textColor="#29262D"
        android:textSize="15sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/quick_add_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="@drawable/florien_widget_primary"
        android:paddingHorizontal="13dp"
        android:paddingVertical="8dp"
        android:text="@string/widget_quick_cta"
        android:textColor="#29262D"
        android:textSize="12sp"
        android:textStyle="bold" />
</LinearLayout>
